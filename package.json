{
  "name": "mobile-app",
  "private": true,
  "version": "0.0.1",
  "description": "mobile-app",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve --ssl",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch"
  },
  "dependencies": {
    "@ionic/core": "4.0.2",
    "socket.io-client": "^2.2.0",
    "webrtc-adapter": "^7.2.1"
  },
  "devDependencies": {
    "@stencil/core": "0.18.0",
    "@types/dom-mediacapture-record": "^1.0.1",
    "@types/socket.io-client": "^1.4.32",
    "@types/webrtc": "0.0.24"
  }
}
